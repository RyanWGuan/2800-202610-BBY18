<%- include('partials/header') %>
<main class="main">
  <br/>

  <div class="detail-page" id="detail-page">
    <div class="detail-loading">Loading recipe…</div>
  </div>

  <!-- <section class="detail-section" id="nutrition-section">
    <div class="detail-section-header">
        <h2>Nutritional Facts</h2>
    </div>
    <div id="nutrition-content">
        <button id="btn-nutrition" onclick="loadNutrition('${meal.strMeal}', '${ingredients.map(i => i.name).join(", ")}')">
          Generate Nutritional Facts
        </button>
    </div>
  </section>` -->

</main>
<%- include('partials/footer') %>